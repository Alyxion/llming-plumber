[project]
name = "llming-plumber"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
    "ruff",
]
